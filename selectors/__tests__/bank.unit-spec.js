let chai = require('chai');
let path = require('path')
chai.should();

import {findBankDomain, findBankLibrary} from '../login/index'

describe('bank selectors', () => {

  it('should find the bank domain')

  it('should find the bank libarary');

})
